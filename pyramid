n = int(input("Enter the number : "))       # for user input 
for i in range(1,n+1):                      # for number in rows
    for j in range (1):                     # for number of spaces before stars (but here use less)
        print(" " *(n-i)+"* "*i, end="")    # { "" } is for spaces before printing of the stars and { n-1 } refers to the number of spaces present before stars and { "* "*i} represent number of stars print multiple of number of rows and { end = ""} printnext row without printing any extra blank row
    print()                                 # this print blank line here 
    
  
    
